#! /bin/sh -ex
source "$(dirname "$0")"/_/env.source.sh

"$local"/webpack-dev-server \
  --path-to-elm-make=node_modules/.bin/elm-make \
  --port=9229 \
  --host=0.0.0.0 \
  --content-base=public/ \
  --inline \
  $@
